# Applies to every volume that has no explicit recurring-job labels (the "default" group).
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: snapshot-daily
  namespace: longhorn-system
spec:
  task: snapshot          # every 6h was needless chain growth (reads walk the snapshot chain); the 01:30 backup snapshots too
  cron: "0 13 * * *"
  retain: 4
  concurrency: 2
  groups: [default]
---
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: backup-daily
  namespace: longhorn-system
spec:
  task: backup            # incremental, block-level, to the Storage Box — encrypted because the volumes are
  cron: "30 1 * * *"      # before kured's reboot window
  retain: 14
  concurrency: 1
  groups: [default]
---
apiVersion: longhorn.io/v1beta2
kind: RecurringJob
metadata:
  name: backup-weekly
  namespace: longhorn-system
spec:
  task: backup
  cron: "0 0 * * 0"
  retain: 8
  concurrency: 1
  groups: [default]
  labels:
    schedule: weekly
